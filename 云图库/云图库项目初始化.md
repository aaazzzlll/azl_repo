```javascript
routes: [
  {
    path: '/',
    name: 'home',
    component: HomeView,
  },
  {
    path: '/about',
    name: 'about',
    // route level code-splitting
    // this generates a separate chunk (About.[hash].js) for this route
    // which is lazy-loaded when the route is visited.
    component: () => import('../views/AboutView.vue'),
  },
],
```
1. 此处两种不同的引入组件方式，component支持直接传入组件，import按需懒加载组件，按需加载可以优化首次打开站点性能

2. 菜单高亮同步问题


3. 封装axios实例：request.ts
	[组件调用接口函数的处理逻辑](../Excalidraw/组件调用接口函数的处理逻辑.md)

4. access.ts实现全局权限校验
	main.ts中引入了这个文件`import '@/access'`
	这叫做副作用导入，一个文件被import，里面的代码就会立即执行
