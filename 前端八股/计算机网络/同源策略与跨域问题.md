---
Date: 2026-03-04
upstream: " 父卡片"
related: 相关卡1
tags:
---
同源策略：浏览器为保证安全性，规定不同域之间的网页不能互相访问，只有在协议、域名、端口都相同的情况下才能交互

跨域问题：当一个网页试图访问另一个域的资源时，就会遇到跨域问题。
常见的解决方法：
	CORS（跨域资源共享）：通过设置服务器的响应头字段`Access-Control-Allow-Origin`,允许特定域的访问
	JSONP：通过`<script>`标签绕过浏览器的同源策略，通常用于GET请求
	