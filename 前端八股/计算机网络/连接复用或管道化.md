### 连接复用
在HTTP/1.0，每请求一个资源就要新建一个TCP连接，频繁创建连接很慢很耗资源。
因此HTTP/1.1默认开启连接复用，**一个TCP连接可以连续发送多个请求**
```plain
建立TCP连接 → 请求HTML → 接收响应 → 请求CSS → 接收响应 → 请求JS → 接收响应 → 关闭连接
              └──────── 复用同一个连接 ────────┘
```


### 管道化
HTTP/1.1支持管道化，允许**一口气连续发送多个请求，不用等前一个响应回来**，但是响应必须严格按请求顺序返回

