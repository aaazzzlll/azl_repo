1. 基本用途
	get用于从服务器获取数据，不会更改服务器上的数据
	post用于向服务器发送数据，通常会更改服务器上的数据

2. 参数传递
	get参数附加在URL中，数据长度有限（URL长度限制）
	post参数通过HTTP请求体传输，没有数据长度限制
3. 浏览器缓存
	get查询到的数据会被浏览器强制缓存
	post通常不会被缓存
4. 速度
	get速度更快，因为其数据嵌入在URL中
	post速度慢一些，因为post请求先发请求头，再发请求体
5. 安全性
	get数据安全性较低，因为参数可以被暴露在URL中
	post安全性较高，参数不会暴露在URL中
