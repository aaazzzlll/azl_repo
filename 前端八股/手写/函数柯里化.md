把一个接收多个参数的函数，转换成一个个只接收一个参数的函数序列

```javascript
function add(a,b,c){
	return a+b+c
}

//柯里化后
function add(a){
	return function(b){
		return function(c){
			return a+b+c
		}
	}
}
```

柯里化的核心价值：
1. 参数复用（固定一部分参数，生成新函数）
2. 延迟执行（参数可以分多次传入）：比如等所有参数到齐了才真正计算
3. 函数组合更友好
4. 提高代码可读性和复用性：把通用函数拆成更小的、可配置的单元