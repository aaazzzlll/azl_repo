---
Date: 2026-03-05
upstream: "[[浏览器进程]]"
related: 相关卡1
tags:
---
渲染进程是多线程，主要包含以下7个线程：
1. **主线程**（GUI渲染和JS引擎共存的线程）
	这是渲染进程中最忙、最重要的线程：
		*解析和渲染页面（GUI渲染）*：读 HTML → 构建 DOM 树 → 读 CSS → 构建样式规则 → 组合成渲染树 → 计算布局（Layout / Reflow） → 绘制（Paint） → 生成待绘制的指令列表。
		*执行JavaScript*：解析JS代码，运行JS（V8引擎就在这里工作）
	关键限制：JS执行时，页面渲染会暂停；渲染
2. **定时器触发线程**
3. **事件触发线程**
4. **异步http请求线程**
5. **合成线程**
6. **I/O线程**
	用来和其他进程进行通信