在 Vue 中，默认情况下模板中的 HTML 注释会在渲染过程中被移除，不会出现在最终的 DOM 中。这是因为 Vue 的模板编译器会将注释视为无用内容并自动删除，以减小最终生成的代码体积。

保留注释方法：
	1. 使用comments选项（Vue 2）：在 Vue 2 中，可以通过在组件选项中设置 `comments: true` 来保留模板中的 HTML 注释

	2. 使用v-pre
	3. 使用特殊注释语法（Vue 3）
