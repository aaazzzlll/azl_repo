队头阻塞：一个慢速或阻塞的元素拖累了整个队列的效率

### HTTP的队头阻塞
发生场景：HTTP 1.1的[连接复用或管道化](连接复用或管道化.md)
**HTTP层的队头阻塞**：HTTP/1.1虽然支持持久连接，但同一条连接内的请求必须按照顺序返回，响应顺序不能变。如果请求1的响应很慢，即使后面的请求已经准备好，也不能提前返回，就会被阻塞

### TCP的队头阻塞
发生场景：TCP的按序交付机制遇到丢包
**TCP层的队头阻塞**：TCP需要保证可靠、有序。只要前面的某个数据包丢失，接收端必须等待重传，所有后续包都被阻塞

